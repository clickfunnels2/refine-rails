#!/usr/bin/env ruby

require "fileutils"

DUMMY_ROOT = File.expand_path("../test/dummy", __dir__)

def system!(*args)
  system(*args) || abort("\n== Command #{args} failed ==")
end

FileUtils.chdir DUMMY_ROOT do
  system!("bin/setup")
end