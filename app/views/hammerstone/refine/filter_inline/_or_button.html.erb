<% 
  criterion = @refine_inline_criterion.deep_dup
  criterion.position = position
  criterion.conjunction = "or"
%>

<%= render "hammerstone/refine/filter_inline/popup_button",
  frame_id: builder,
  src: hammerstone_refine_conditions_path(criterion.attributes) do
%>
  + Add "OR" Group
<% end %>
