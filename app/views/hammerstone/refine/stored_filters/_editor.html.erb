<%= turbo_frame_tag :stored_filters do %>
  <%= content_tag :div, data: { controller: 'refine--stored-filter', refine__stored_filter_id_value: @stored_filter&.id,
    refine__stored_filter_stable_id_value: @refine_filter&.configuration[:stable_id], 
    refine__stored_filter_filter_name_value: @refine_filter.class.name,
    action: 'filter-stabilized@window->refine--stored-filter#activateSaveLink filter-stabilized@window->refine--stored-filter#updateStableIdField'
  } do %>
    <%= render partial: "head" %>
    <%= render partial: "hammerstone/refine_blueprints/query" %>
  <% end %>
<% end %>
