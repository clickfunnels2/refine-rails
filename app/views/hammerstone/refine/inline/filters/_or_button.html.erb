<% 
  criterion = Hammerstone::Refine::Inline::Criterion.new(
    stable_id: @refine_filter.to_stable_id,
    refine_filter: @refine_filter,
    client_id: @refine_client_id,
    conjunction: "and",
    position: position
  )
%>

<%= render "hammerstone/refine/inline/filters/popup_button",
  frame_id: builder,
  src: hammerstone_refine_conditions_path(criterion.attributes) do
%>
  + Add "OR" Group
<% end %>
