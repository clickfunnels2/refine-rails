#!/usr/bin/env ruby

require "fileutils"

def system!(*args)
  system(*args) || abort("\n== Command #{args} failed ==")
end

system! "yarn build"
system! "bin/dummy bin/setup"

puts "\n== DONE! ==\n"
puts <<~MSG
  You can now run the dummy app using 

    bin/dev
MSG
